# Domain configuration
# Your main domain name (e.g., example.com)
DOMAIN=example.com
# Admin panel subdomain (will be prefixed with 'admin.')
ADMIN_DOMAIN=admin.example.com
# Let's Encrypt configuration
# Email for Let's Encrypt notifications and account recovery
LETSENCRYPT_EMAIL=your-email@example.com
# Ports configuration
# Port for VLESS + XTLS protocol
XRAY_VLESS_PORT=8443
# Port for WebSocket protocol
XRAY_WS_PORT=8080
# Port for gRPC protocol
XRAY_GRPC_PORT=2087
# Admin panel configuration
# Port for the admin panel web interface
ADMIN_PANEL_PORT=3000


# Optional: Additional configuration
# Uncomment and set these if you want to customize the setup

# Database configuration
# DB_PATH=/app/data/users.db

# Logging configuration
# LOG_LEVEL=info

# Security configuration
# RATE_LIMIT=100
# MAX_CONNECTIONS=1000
